import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return <div>Hello, Retail Expense Sales Tracker!</div>;
};

ReactDOM.render(<App />, document.getElementById('root'));